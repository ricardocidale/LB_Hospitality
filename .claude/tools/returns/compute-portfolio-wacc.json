{
  "name": "compute_portfolio_wacc",
  "description": "Computes the capital-weighted WACC across a portfolio of properties. Each property's WACC is weighted by its share of total portfolio capital.",
  "input_schema": {
    "type": "object",
    "properties": {
      "properties": {
        "type": "array",
        "description": "Array of property objects with name, equity, debt, cost_of_equity, cost_of_debt",
        "items": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "equity": { "type": "number" },
            "debt": { "type": "number" },
            "cost_of_equity": { "type": "number" },
            "cost_of_debt": { "type": "number" }
          },
          "required": ["name", "equity", "debt", "cost_of_equity", "cost_of_debt"]
        }
      },
      "tax_rate": {
        "type": "number",
        "description": "Corporate tax rate as decimal (uniform across properties)"
      }
    },
    "required": ["properties", "tax_rate"]
  }
}
